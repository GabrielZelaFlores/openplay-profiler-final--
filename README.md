# OpenPlay Profiler Final

Proyecto de Mineria de Datos para analizar OpenPlay mediante profiling, vector de caracteristicas, reduccion dimensional y clustering.

La aplicacion principal esta en:

```text
openplay-profiler-final/
```

Para ejecutar:

```bash
cd openplay-profiler-final
npm install
npm run dev
```